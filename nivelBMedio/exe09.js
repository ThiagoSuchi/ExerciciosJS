// 09 - Escreva uma função que remove duplicatas de um array.

function removendoDuplicadas(num) {
    var numerosDiferentes = []
    for (let i = 0; i < num.length; i++) {
        for (let j = 0; j < num.length ; j++) {
           
        
        }
    }

    return numerosDiferentes
}

console.log(removendoDuplicadas([2, 3, 3, 4, 5, 2, 10, 8, 9, 7, 10]));